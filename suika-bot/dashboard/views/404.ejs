<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/nav', { 
        title: title,
        isAuthenticated: typeof user !== 'undefined' && user,
        botClient: typeof botClient !== 'undefined' ? botClient : null
    }) %>
    <div class="login-container">
        <div class="login-card" style="text-align: center;">
            <h1 style="font-size: 4rem; color: var(--primary-blue);">404</h1>
            <h2>Page Not Found</h2>
            <p style="color: var(--text-secondary); margin: 20px 0;">The page you're looking for doesn't exist.</p>
            <a href="/" class="btn">Go Home</a>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
