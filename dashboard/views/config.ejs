<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configuration - Discord Bot Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/nav', { active: 'config', user: user }) %>
    
    <div class="container">
        <h1>Bot Configuration</h1>
        
        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>
        
        <% if (success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <div class="config-form">
            <form method="POST" action="/config">
                <div class="form-group">
                    <label for="prefix">Bot Prefix</label>
                    <input type="text" id="prefix" name="prefix" value="<%= config.prefix %>" required>
                    <small>The command prefix for the bot (e.g., !, ?, $)</small>
                </div>
                
                <div class="form-group">
                    <label for="language">Default Language</label>
                    <select id="language" name="language">
                        <option value="en" <%= config.language === 'en' ? 'selected' : '' %>>English</option>
                        <option value="vi" <%= config.language === 'vi' ? 'selected' : '' %>>Vietnamese</option>
                    </select>
                    <small>The default language for bot responses</small>
                </div>
                
                <div class="form-group">
                    <label for="adminBot">Bot Admins</label>
                    <textarea id="adminBot" name="adminBot" rows="4"><%= config.adminBot.join(', ') %></textarea>
                    <small>Comma-separated list of admin user IDs</small>
                </div>
                
                <button type="submit" class="btn btn-primary">Save Configuration</button>
            </form>
        </div>
        
        <div class="config-info">
            <h2>Configuration Help</h2>
            <ul>
                <li><strong>Prefix:</strong> The character(s) used before commands (e.g., !help, ?ping)</li>
                <li><strong>Language:</strong> Sets the default language for bot messages</li>
                <li><strong>Admins:</strong> Users who have full access to administrative commands</li>
            </ul>
            <p class="warning">⚠️ Changes to configuration will take effect immediately but may require a bot restart for some features.</p>
        </div>
    </div>
</body>
</html>
