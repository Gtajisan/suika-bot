<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="navbar-brand">🍈 Suika Bot</a>
            <ul class="navbar-menu">
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/commands" class="active">Commands</a></li>
                <li><a href="/features">Features</a></li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h1>⚡ All Commands (<%= commands.length %>)</h1>
        
        <div class="commands-grid">
            <% commands.forEach(cmd => { %>
            <div class="command-card">
                <h3>/<%= cmd.name %></h3>
                <p><%= cmd.description %></p>
                <% if (cmd.aliases && cmd.aliases.length > 0) { %>
                <p class="aliases">Aliases: <%= cmd.aliases.join(', ') %></p>
                <% } %>
                <span class="badge"><%= cmd.category %></span>
            </div>
            <% }); %>
        </div>
    </div>

    <footer>
        <p>&copy; 2025 Suika Bot | Made by Gtajisan</p>
    </footer>
</body>
</html>
